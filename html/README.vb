EVENTS
 
    onkeyup -> si attiva quando si rilascia il tasto della tastiera sull’elemento HTML.

    • button

        onclick -> si attiva quando si clicca sull'elemento.

TAGS

    - <input> -> per inserire un input. 

        <input type=" "> N.B: Non ha bisogno del tag di chiusura.

            • "button"
            • "checkbox"
            • "date"
            • "email"
            • "file"
            • "image"
            • "password"
            • "text"
            • "url"

    - <table> -> per creare una tabella.

        • <tr> -> per definire una riga.
        • <th> -> per definire un header.
        • <td> -> per definire un dato/cella.

        
